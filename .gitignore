# dataset — supplied by the organisers, not ours to redistribute
MoveInSync - Anonymised Trip-Log Dataset/
# the same dataset dropped at the repo root: ~570 MB, never belongs in git
*.csv
*.pdf

# secrets
.env
agent/.env

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# scratch and generated output
scratch/
*.parquet

# node
node_modules/
web/dist/
.gstack/
